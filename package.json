{
  "private": true,
  "engines": {
    "node": "*",
    "npm": "*"
  },
  "dependencies": {
    "pulp": "*",
    "bower": "*"
  },
  "scripts": {
    "postinstall": "pulp --version && bower --version && bower update",
    "build": "psc \"bower_components/purescript-*/src/**/*.purs\" \"bower_components/purescript-*/src/**/*.js\" && pulp build --to html/Main.js",
    "test": "pulp test",
    "watch": "pulp -w test"
  }
}
