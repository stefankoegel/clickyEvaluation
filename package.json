{
  "private": true,
  "engines": {
    "node": "*",
    "npm": "*"
  },
  "dependencies": {
    "pulp": "*",
    "bower": "*"
  },
  "scripts": {
    "postinstall": "pulp --version && bower --version && bower update",
    "build": "pulp build --to html/Main.js",
    "test": "pulp test",
    "watch": "pulp -w test"
  }
}
