{
  "private": true,
  "engines": {
    "node": "*",
    "npm": "*"
  },
  "dependencies": {
    "purescript": "0.10.1",
    "pulp": "9.0.1",
    "bower": "*"
  },
  "scripts": {
    "postinstall": "pulp --version && bower --version && bower update",
    "build": "pulp build --to html/libs/Main.js",
    "test": "pulp test",
    "watch": "pulp -w test"
  }
}
